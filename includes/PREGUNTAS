PREGUNTAS

al final en addIteToCart uso el item.product_id de shopify ?
intervalo de tiempo en tracking nos
el codigo de customer que tengo que usar para preguntar x un customer y guardarlo cual es ? (estoy usando infoReturned['shopifyInfo'].customer.email porque el otro me viene en null) *
porque infoReturned["bodyGetCustomerDetails"].addressCode viene undefined (tal vez a veces)?
valor del docType en getShippingTrackongNumber ?

ARREGLAR
arreglar la condicion de parada del shipping tracking nos
arreglar el intervalo de tiempo en tracking nos
mirar bien el tema de CreateOrder en el campo ShipAddressCode , porque a veces no llega ? , debe ser con waterfall ? , asincrono ?